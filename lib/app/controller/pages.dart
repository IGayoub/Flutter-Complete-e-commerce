enum PageApp {
  optionAuth,
  signInscr,
  signUpscr,
  home,
}

enum PageAuth { opsignIn, opsignUp, optionBack, signIn, signUp }

enum PageHome { menu, menuClose, notifications, details,backDetails,seeAllFeatured, seeAllBestSell }

enum PageMenu {profile,myCart,favourit,myOrders,language,settings}