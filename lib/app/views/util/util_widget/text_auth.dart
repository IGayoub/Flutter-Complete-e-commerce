import 'package:flutter/material.dart';

class Util {
  static Widget $textAuth($text) {
    return Text(
      $text,
      style: const TextStyle(
        color: Color(0xff434343),
        fontWeight: FontWeight.normal,
        fontSize: 30,
      ),
    );
  }
}
